{
  "name": "@openclaw/azure-dab",
  "version": "0.1.0",
  "type": "module",
  "description": "Provision free Azure SQL databases and expose them via Data API Builder (REST/GraphQL/MCP)",
  "license": "MIT",
  "openclaw": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.0"
  },
  "peerDependencies": {
    "openclaw": ">=2026.3.0"
  },
  "devDependencies": {
    "vitest": "^4.1.2"
  }
}
